name: app_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  grpc: ^3.0.2
  protobuf: ^2.1.0
  mongo_dart: ^0.7.4+1
  crypto: ^3.0.2

dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.15.0
